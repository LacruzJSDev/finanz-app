---
description: Creates or refines a SDD feature specification and implementation plan.
agent: spec-author
---

Create or update the feature documentation for: $ARGUMENTS

Read `AGENTS.md`, the constitution, `specs/README.md`, and the templates. Create a numbered feature directory if it does not exist. Produce a testable `spec.md`, a dependency-aware `plan.md`, and atomic task files. Do not implement application code.
