// El vocabulario que core publica al resto de la aplicación. El cliente
// generado queda como detalle interno de core: si mañana cambia el generador
// o se pasa a otro transporte, esto es lo único que se toca.
// Aquí solo hay modelos, nunca servicios de api.
export * from '../api/model/models';
